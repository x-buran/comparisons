# Буран для программистов Рефала

## Введение

Если вы знаете Рефал, вы уже понимаете главное: вычисление есть преобразование образцов. Буран развивает эту идею, сохраняя философию Рефала и добавляя современные возможности.

Этот документ поможет вам перейти от Рефала к Бурану, показывая соответствия между знакомыми конструкциями и новым синтаксисом.

---

## Основные соответствия

### Функции

**Рефал:**

```refal
Factorial {
    0 = 1;
    s.N = <* s.N <Factorial <- s.N 1>>>;
}
```

**Буран:**

```
факториал {
    [0] ↦ [1]
    [𝑛] ↦ [𝑛 × факториал(𝑛 − 1)]
}
```

Ключевые различия:

- Стрелка `↦` вместо `=`
- Квадратные скобки `[ ]` для образцов
- Вызов функции через круглые скобки: `факториал(𝑛 − 1)`
- Математическая нотация: `×` вместо `*`, `−` вместо `-`
- Unicode-идентификаторы: `факториал` вместо `Factorial`

### Переменные

**Рефал** использует префиксы для типов переменных:

- `s.X` — символьная (один терм)
- `t.X` — термовая (один терм в скобках или символ)
- `e.X` — выражение (любое количество термов)

**Буран** определяет тип переменной по контексту:

- `𝑥` — обычная переменная (связывает значение)
- `⟨𝑥⟩` — переменная в зеркальных скобках (захватывает образец или остаток)

```
# Рефал: e.Rest захватывает хвост
Length {
    /* пусто */ = 0;
    t.Head e.Rest = <+ 1 <Length e.Rest>>;
}

# Буран: ⟨rest⟩ захватывает остальные элементы
длина {
    [list: ] ↦ [0]
    [list: _, ⟨rest⟩] ↦ [1 + длина(rest)]
}
```

### Образцы и конструкторы

**Рефал:**

```refal
(Point s.X s.Y)
(RGB s.R s.G s.B)
```

**Буран:**

```
[point: 𝑥, 𝑦]
[rgb: 𝑟, 𝑔, 𝑏]
```

В Буране образцы всегда заключены в квадратные скобки. Первый элемент — имя конструктора, за которым следует двоеточие и аргументы через запятую.

---

## Угловые и зеркальные скобки

В Рефале угловые скобки `< >` используются для вызова функций. В Буране эту роль выполняют круглые скобки, а угловые (`⟨ ⟩` — зеркальные) имеют особое назначение.

### Зеркальные скобки ⟨ ⟩

Зеркальные скобки в Буране обеспечивают:

1. **Захват образцов** (аналог `e.` в Рефале)
2. **Доступ к метаданным** (идентичность)
3. **Проверку типов**

```
# Захват остатка списка
[list: первый, ⟨остальные⟩] ↦ обработать(остальные)

# Получение метаданных значения
⟨[rgb: 255, 0, 0]⟩ ↦ метаданные

# Проверка типа
⟨𝑥 | "type": "integer"⟩ ↦ обработать-целое(𝑥)
```

---

## Охранные условия (Guards)

Рефал не имеет встроенных охранных условий — проверки выполняются через сопоставление с образцом или вспомогательные функции.

Буран добавляет охранные условия через вертикальную черту `|`:

```
абс {
    [𝑛 | 𝑛 ≥ 0] ↦ [𝑛]
    [𝑛] ↦ [−𝑛]
}

классифицировать {
    [𝑛 | 𝑛 > 0] ↦ ["положительное"]
    [𝑛 | 𝑛 < 0] ↦ ["отрицательное"]
    [0] ↦ ["ноль"]
}
```

Условия можно комбинировать:

```
в-диапазоне {
    [𝑛 | 𝑛 ≥ 0 ∧ 𝑛 ≤ 100] ↦ [✔]
    _ ↦ [✘]
}
```

---

## Ввод-вывод

**Рефал** (Рефал-5):

```refal
$ENTRY Go {
    = <Prout 'Hello, World!'>;
}
```

**Буран:**

```
["Привет, мир!"] ↦ [stdout]
```

Или в обратном направлении:

```
[stdout] ↤ ["Привет, мир!"]
```

### Чтение из файла

**Рефал:**

```refal
<Open 'r' 1 'data.txt'>
<Get 1> : e.Line
```

**Буран:**

```
[File: "data.txt"] ↦ содержимое
```

### Работа с форматами

Буран имеет встроенную поддержку форматов:

```
[File: "config.json" :: json] ↦ данные
данные ↦ [File: "output.yaml" :: yaml]
```

---

## Списки

**Рефал** использует неявную структуру списка — термы просто следуют друг за другом.

**Буран** использует явный конструктор `list:`:

```
# Рефал
1 2 3 4 5

# Буран
[list: 1, 2, 3, 4, 5]
```

### Операции со списками

```
# Рефал
Map {
    s.Fn /* пусто */ = /* пусто */;
    s.Fn t.Head e.Rest = <Mu s.Fn t.Head> <Map s.Fn e.Rest>;
}

# Буран
map {
    _, [list: ] ↦ [list: ]
    f, [list: голова, ⟨хвост⟩] ↦ [list: f(голова), ⟨map(f, хвост)⟩]
}
```

Использование:

```
map({ 𝑥 ↦ 𝑥 × 2 }, [list: 1, 2, 3]) ↦ [list: 2, 4, 6]
```

---

## Блоки образцов

Буран добавляет **блоки образцов** — анонимные функции, которые можно передавать как аргументы:

```
{ 𝑥 ↦ 𝑥 × 2 }           # удваивает значение
{ [𝑎, 𝑏] ↦ [𝑎 + 𝑏] }    # складывает пару
```

Это позволяет писать более выразительный код:

```
# Фильтрация чётных чисел
filter({ 𝑛 | 𝑛 mod 2 = 0 }, [list: 1, 2, 3, 4, 5, 6])
↦ [list: 2, 4, 6]

# Преобразование с условием
map({ 𝑛 | 𝑛 > 0 ↦ 𝑛, _ ↦ 0 }, данные)
```

---

## Типы и идентичность

Рефал — нетипизированный язык. Буран добавляет систему типов через **идентичность** — метаданные, привязанные к образцам.

### Определение типа

```
⟨[rgb: _, _, _]⟩ ↤ [identity:
    "type": "Цвет",
    "description": "RGB-цвет с тремя компонентами"
]
```

### Проверка типа

```
обработать {
    ⟨𝑥 | "type": "Цвет"⟩ ↦ отрисовать(𝑥)
    ⟨𝑥 | "type": "integer"⟩ ↦ вычислить(𝑥)
    _ ↦ [error: неизвестный тип]
}
```

---

## Сравнительная таблица

| Концепция             | Рефал          | Буран                 |
| --------------------- | -------------- | --------------------- |
| Функция               | `Func { ... }` | `функция { ... }`     |
| Вызов функции         | `<Func arg>`   | `функция(арг)`        |
| Присваивание          | `= результат;` | `↦ [результат]`       |
| Символьная переменная | `s.X`          | `𝑥` (по контексту)    |
| Выражение-переменная  | `e.X`          | `⟨𝑥⟩`                 |
| Скобки структуры      | `( ... )`      | `[конструктор: ...]`  |
| Комментарий           | `/* ... */`    | `# ...`               |
| Пустое выражение      | `/* пусто */`  | `[]` или `[list: ]`   |
| Печать                | `<Prout ...>`  | `... ↦ [stdout]`      |
| Чтение файла          | `<Get ...>`    | `[File: "..."] ↦ ...` |

---

## Пример: обращение списка

**Рефал:**

```refal
Reverse {
    /* пусто */ = /* пусто */;
    t.First e.Rest = <Reverse e.Rest> t.First;
}
```

**Буран:**

```
обратить {
    [list: ] ↦ [list: ]
    [list: первый, ⟨остальные⟩] ↦ [list: ⟨обратить(остальные)⟩, первый]
}
```

---

## Пример: сортировка вставками

**Рефал:**

```refal
Sort {
    /* пусто */ = /* пусто */;
    t.X e.Rest = <Insert t.X <Sort e.Rest>>;
}

Insert {
    t.X /* пусто */ = t.X;
    t.X t.Y e.Rest, <Compare t.X t.Y>: {
        '<' = t.X t.Y e.Rest;
        s.Other = t.Y <Insert t.X e.Rest>;
    };
}
```

**Буран:**

```
сортировка {
    [list: ] ↦ [list: ]
    [list: 𝑥, ⟨остальные⟩] ↦ вставить(𝑥, сортировка(остальные))
}

вставить {
    𝑥, [list: ] ↦ [list: 𝑥]
    𝑥, [list: 𝑦, ⟨остальные⟩] | 𝑥 ≤ 𝑦 ↦ [list: 𝑥, 𝑦, ⟨остальные⟩]
    𝑥, [list: 𝑦, ⟨остальные⟩] ↦ [list: 𝑦, ⟨вставить(𝑥, остальные)⟩]
}
```

---

## Математика в Буране

Рефал имеет базовую арифметику через встроенные функции вроде `<* ...>`, `<+ ...>`. Буран предлагает богатую математическую нотацию, следующую ISO 80000-2 и использующую стандартные математические символы.

### Арифметические операции

**Рефал:**

```refal
<+ 2 3>     /* сложение */
<* 3 4>     /* умножение */
</ 8 2>     /* деление */
<Mod 10 3>  /* остаток */
```

**Буран:**

```
[2 + 3] ↦ [5]       # сложение
[3 × 4] ↦ [12]      # умножение (U+00D7)
[8 ÷ 2] ↦ [4]       # деление (U+00F7)
[10 mod 3] ↦ [1]    # остаток
```

### Степени и корни

Буран использует Unicode-символы для корней и надстрочные цифры для степеней:

```
# Корни
[√16] ↦ [4]         # квадратный корень (U+221A)
[∛27] ↦ [3]         # кубический корень (U+221B)
[∜16] ↦ [2]         # корень четвёртой степени (U+221C)
[⁵√32] ↦ [2]        # корень пятой степени

# Степени
[𝑥²] ↦ [𝑥 × 𝑥]       # квадрат через надстрочный символ
[2³] ↦ [8]          # куб
[10⁴] ↦ [10000]     # любая степень: ⁰¹²³⁴⁵⁶⁷⁸⁹
[𝑥⁻¹] ↦ [1⁄𝑥]       # отрицательные степени
```

### Математические константы

Буран предоставляет встроенные математические константы:

```
𝛑    # пи (3.14159...)
𝐞    # число Эйлера (2.71828...)
𝛗    # золотое сечение (1.61803...)
∞    # бесконечность
𝐢    # мнимая единица

# Использование
[2𝛑] ↦ [6.28318...]
[𝐞^2] ↦ [7.38905...]
[𝐞^(𝐢𝛑)] ↦ [−1]     # тождество Эйлера
```

### Рациональные числа

Буран поддерживает точную арифметику с дробями:

```
# Ввод и вывод дробей
[1/3 + 1/3] ↦ [⅔]       # автоматически упрощается
[½ + ¼] ↦ [¾]           # Unicode-дроби
[⅓ × ¾] ↦ [¼]           # умножение дробей

# Преобразование
[real: 1/3] ↦ [0.333...]    # в десятичное
[rational: 0.75] ↦ [¾]      # в дробь
```

### Типографика переменных (ISO 80000-2)

Буран различает типы через начертание шрифта:

```
# Обычные переменные (ASCII)
x, result, counter, max

# Математические переменные (курсив)
𝑥, 𝑦, 𝑧, 𝑎, 𝑏, 𝑛

# Математические константы (прямой жирный)
𝐢, 𝐞, 𝛑, 𝐜

# Векторы и матрицы (жирный курсив)
𝒗, 𝒂, 𝑨, 𝑩

# Пример различия:
sin(𝑥)      # функция синус
𝑠𝑖𝑛 ↦ 0.5   # переменная с именем «sin»
```

### Сравнения и логические операции

```
# Сравнения
[𝑥 > 𝑦], [𝑥 ≥ 𝑦], [𝑥 < 𝑦], [𝑥 ≤ 𝑦]
[𝑥 = 𝑦], [𝑥 ≠ 𝑦]
[𝑥 ≈ 𝑦]         # приблизительно равно (U+2248)
[𝑥 ≪ 𝑦]         # намного меньше (U+226A)
[𝑥 ≫ 𝑦]         # намного больше (U+226B)

# Логические операции
[𝑝 ∧ 𝑞]         # И (конъюнкция)
[𝑝 ∨ 𝑞]         # ИЛИ (дизъюнкция)
[¬ 𝑝]           # НЕ (отрицание)
[𝑝 → 𝑞]         # импликация
[𝑝 ↔ 𝑞]         # эквивалентность
```

### Операции с множествами

```
[𝑥 ∈ 𝑆]         # принадлежность множеству
[𝐴 ∪ 𝐵]         # объединение
[𝐴 ∩ 𝐵]         # пересечение
[𝐴 ⊂ 𝐵]         # строгое подмножество
[𝐴 ⊆ 𝐵]         # подмножество или равно
[𝐴 ∖ 𝐵]         # разность множеств
[∅]             # пустое множество
```

### Суммы и произведения

```
[∑(𝑖=1 to 10) 𝑖] ↦ [55]        # сумма от 1 до 10
[∏(𝑖=1 to 5) 𝑖] ↦ [120]        # произведение (5!)
[1, 2, 3, …, 100]              # последовательность
```

### Тригонометрия и специальные функции

```
[sin(𝛑⁄4)] ↦ [√2⁄2]
[cos(0)] ↦ [1]
[tan(45°)] ↦ [1]            # градусная мера
[90°] ↦ [𝛑⁄2]               # преобразование в радианы
[exp(1)] ↦ [𝐞]
[ln(𝐞)] ↦ [1]
[log(100)] ↦ [2]            # десятичный логарифм
```

### Округление

```
[⌊3.7⌋] ↦ [3]       # пол (округление вниз)
[⌈3.2⌉] ↦ [4]       # потолок (округление вверх)
[⌊3.5⌉] ↦ [4]       # округление к ближайшему
```

### Домены (специализированные области)

Для расширенных математических операций Буран использует **аннотации доменов**:

```
# Матричные операции
[𝑨 × 𝑩 :: matrix] ↦ произведение-матриц
[det(𝑨) :: matrix] ↦ определитель

# Комплексные числа
[log(−1) :: complex] ↦ [𝐢𝛑]

# Символьные вычисления
[∂f/∂𝑥 :: symbolic] ↦ производная
[∫ sin(𝑥) d𝑥 :: symbolic] ↦ [−cos(𝑥) + 𝐶]

# Статистика
[mean([1, 2, 3, 4, 5]) :: statistical] ↦ [3]
```

### Числовые множества как домены

```
ℕ    # натуральные числа {0, 1, 2, ...}
ℤ    # целые числа
ℚ    # рациональные числа
ℝ    # вещественные числа
ℂ    # комплексные числа
```

### Сравнение с Рефалом

| Концепция | Рефал                   | Буран                |
| --------- | ----------------------- | -------------------- |
| Сложение  | `<+ a b>`               | `[𝑎 + 𝑏]`            |
| Умножение | `<* a b>`               | `[𝑎 × 𝑏]`            |
| Степень   | `<Power n k>`           | `[𝑛ᵏ]` или `[𝑛 ^ 𝑘]` |
| Корень    | —                       | `[√𝑛]`, `[∛𝑛]`       |
| Дробь     | —                       | `[½]`, `[⅔]`         |
| Константы | —                       | `𝛑`, `𝐞`, `𝛗`, `𝐢`   |
| Сравнение | вспомогательная функция | `[𝑥 ≤ 𝑦]`, `[𝑥 ≠ 𝑦]` |

Буран позволяет писать математические выражения так, как они выглядят в учебниках — это делает код читаемым как для людей, так и для систем ИИ.

---

## Главные преимущества Бурана

1. **Unicode-идентификаторы** — пишите код на любом языке
2. **Математическая нотация** — используйте `×`, `÷`, `≤`, `≥`, `∧`, `∨`
3. **Охранные условия** — выразительные проверки без вспомогательных функций
4. **Блоки образцов** — анонимные функции для функционального стиля
5. **Система типов** — опциональная типизация через идентичность
6. **Встроенный I/O** — простая работа с файлами и форматами
7. **Зеркальные скобки** — мощная рефлексия и метапрограммирование

---

## Философское единство

Буран и Рефал разделяют одну философию: **вычисление есть преобразование образцов**.

Рефал родился в 1966 году и доказал, что эта идея работает. Буран рождается в 2025 году и адаптирует её для эпохи искусственного интеллекта — когда машины пишут код вместе с людьми.

Если вы освоили Рефал, переход к Бурану будет естественным. Синтаксис новый, но душа та же — образцы, преобразующиеся в образцы.

---

*Язык Буран находится в разработке. Спецификация и эталонная реализация ожидаются в начале 2026 года.*

© 2026 Danslav Slavenskoj
